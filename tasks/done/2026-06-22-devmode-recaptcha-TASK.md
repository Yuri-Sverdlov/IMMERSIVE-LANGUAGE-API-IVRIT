# TASK — активное задание (ARCHIVED)

**От:** архитектор (чат)  
**Кому:** кодер (терминал)  
**Дата:** 2026-06-22  
**Статус:** ACCEPTED

## Цель

Убрать блокер локального запуска: при `DEV_MODE=true` бэкенд не должен требовать
GCP ADC только из-за инициализации reCAPTCHA на уровне модуля.

## Критерии приёмки

- [x] `python -m uvicorn server.main:app` стартует при `DEV_MODE=true` без ADC
- [x] При `DEV_MODE=false` код reCAPTCHA не сломан (ревью по diff, без обязательного prod-теста)
- [x] Минимальный diff, без рефакторинга «заодно»
- [x] `REPORT.md` обновлён
