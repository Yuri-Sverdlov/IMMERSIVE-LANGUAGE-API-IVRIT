# TASK — активное задание (007) — ARCHIVED / ACCEPTED

**Статус:** ACCEPTED (архитектор, 2026-06-22)

## Критерии приёмки

- [x] `app/immergo.config.json` с дефолтами RU/HE, 420s, 2000ms
- [x] `GET /api/config` работает
- [x] `SESSION_TIME_LIMIT` читается из config
- [x] Дефолты языков в UI — из config (если нет localStorage)
- [x] `silence_duration_ms` — из config в Gemini setup
- [x] QUICK_START + `.env.example` обновлены
- [x] Commit + push `f60c7df`; `app/.env` не в git
