# ACCEPTED — вердикт архитектора

- **Дата:** 2026-06-21
- **Задание:** Починить живой голос: убрать `proactivity`, сделать UTF-8-вывод постоянным.
- **Вердикт:** **ПРИНЯТО ПОЛНОСТЬЮ.** Пользователь подтвердил живой голосовой диалог на иврите.

## Что проверено объективно

- `app/server/main.py` стр.17-18: `sys.stdout/stderr.reconfigure(encoding="utf-8", errors="replace")`.
- `app/server/gemini_live.py`: `proactivity` — 0 совпадений (поле больше не отправляется).
- Backend стартует чисто, `GET /api/status` = 200, в логе нет `1007 ... proactivity`.
- **Живой тест пользователя:** сессия прошла, ИИ отвечает голосом на иврите.

## Замечания (не блокирующие, на будущее)

- Пользователь отметил задержки ответа и обрывы сессии. Вероятная причина — бесплатная квота
  Live API и нестабильность preview-модели `gemini-3.1-flash-live-preview`. Не дефект кода.
  Возможные будущие шаги: тюнинг VAD/таймаутов, проверка лимитов ключа, billing.
