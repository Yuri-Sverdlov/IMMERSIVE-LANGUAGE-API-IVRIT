# PROJECT_LOG.md — журнал проекта (append-only)

> Хронология всех сессий. Только добавляем снизу, не переписываем прошлое.
> «Что было» живёт здесь, «что сейчас» — в `tasks/REPORT.md`.

Формат записи:

```
## YYYY-MM-DD — короткий заголовок
- Кто: архитектор / кодер
- Задание: ссылка/суть TASK
- Сделано: ...
- Проверено: ...
- Решения / грабли: ...
```

---

## 2026-06-21 — Инициализация двухагентной схемы
- Кто: архитектор (чат)
- Сделано: скопирован `DEV-NOTES.md`, создан канонический комплект файлов
  (`AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, `PROJECT_LOG.md`, `tasks/TASK.md`,
  `tasks/REPORT.md`, `tasks/done/`).
- Проверено: структура создана в корне проекта.
- Дальше: заполнить `CONTEXT.md` (суть/стек) и первое `tasks/TASK.md`.

## 2026-06-21 — План: установка Immergo + настройка на иврит
- Кто: архитектор (чат)
- Проект определён: апстрим ZackAkil/immersive-language-learning-with-live-api ("Immergo"),
  кейс — учим иврит, родной русский.
- Разведка апстрима (через GitHub raw/API): языки и дефолты заданы в
  `src/components/view-missions.js` (иврита в списке НЕТ); системный промпт собирается
  на фронте (`view-chat.js`), бэкенд (`gemini_live.py`) лишь передаёт его в Live API.
  Запуск требует GCP Vertex AI + ADC. Bash-скрипты репо не идут на Windows -> ручные команды.
- Решения архитектора: код приложения -> подпапка `app/`; иврит добавляем как `🇮🇱 Hebrew`;
  дефолты to=Hebrew, from=Russian; локально `DEV_MODE=true`.
- Сделано: заполнены `CONTEXT.md`, `AGENTS.md` (раздел запуска), написан `tasks/TASK.md`
  (5 этапов с блокером на GCP), `tasks/REPORT.md` подготовлен.
- Дальше: кодер выполняет этапы 1–4; этап 5 — после получения GCP PROJECT_ID + ADC.

## 2026-06-21 — Приёмка этапов 1–4 (установка + настройка иврита)
- Кто: архитектор (чат), проверка отчёта кодера.
- Проверено объективно (host-Read + Test-Path + build-лог):
  - `app/` = клон апстрима; `node_modules/` и `dist/` присутствуют.
  - `app/src/components/view-missions.js`: стр.45 `<option>🇮🇱 Hebrew</option>` (флаг корректный, UTF-8);
    стр.285 дефолт to-lang `includes('Hebrew')`; стр.295 дефолт from-lang `includes('Russian')`.
  - `npm run build` зелёный (15 модулей, 243ms). `pip install` без ошибок.
- Вердикт: **этапы 1–4 ПРИНЯТЫ**. Этап 5 заблокирован внешне (нет gcloud CLI, PROJECT_ID = заглушка) —
  не дефект кодера.
- Косметика (не блокирует): имена переменных `frenchOption`/`englishOption` стали неточными
  (теперь Hebrew/Russian) — переименовать при следующем касании файла.
- Задание НЕ архивируем: TASK остаётся открытым на этап 5 до получения GCP PROJECT_ID + установки gcloud.
- Дальше: решение пользователя по GCP (см. вопрос в чате).

## 2026-06-21 — Смена курса: Vertex AI -> Gemini API key; архив задания 001
- Кто: архитектор (чат).
- Решение пользователя: GCP не использовать -> идём через Gemini Developer API (ключ из AI Studio).
  gcloud-установка ОТМЕНЕНА (на этом пути не нужна).
- Разведка (ai.google.dev): Live API работает по `genai.Client(api_key=...)`; актуальная live-модель
  `gemini-3.1-flash-live-preview`; зашитая `gemini-live-2.5-flash-native-audio` — Vertex-имя, устарела.
  Клиент в репо создаётся жёстко в Vertex-режиме (`gemini_live.py`, `__init__`).
- Архив: этапы 1-4 задания 001 приняты -> `tasks/done/001-install-immergo-hebrew/`
  (TASK + REPORT + ACCEPTED). Vertex-этап 5 снят.
- Новое задание 002 (`tasks/TASK.md`): добавить в `GeminiLive` ветку `api_key`, читать `GEMINI_API_KEY`
  в `main.py`, сменить дефолт MODEL на `gemini-3.1-flash-live-preview`, перевести `.env` в developer-режим,
  переименовать косметические переменные. Этапы 1-3 — сразу; этап 4 (запуск) — после получения ключа.
- Блокер теперь один: пользователь даёт `GEMINI_API_KEY` (aistudio.google.com).

## 2026-06-21 — Проверка задания 002 + запуск серверов
- Кто: архитектор (чат). Ключ вставлен пользователем в `app/.env`.
- Проверено независимо (Grep/Read + terminal-лог кодера + повторный запуск):
  - `gemini_live.py`: `__init__(... api_key=None)`, ветка `genai.Client(api_key=...)` (стр.27/36-39).
  - `main.py`: `MODEL` дефолт `gemini-3.1-flash-live-preview` (стр.56), `GEMINI_API_KEY` (стр.57),
    `api_key=GEMINI_API_KEY` в `GeminiLive(...)` (стр.260).
  - `.env`: `MODEL=gemini-3.1-flash-live-preview`, `GEMINI_API_KEY` присутствует, `DEV_MODE=true`.
  - `view-missions.js`: переменные переименованы (`hebrewOption`/`russianOption`).
  - Доп-фикс кодера: `recaptcha_validator.py` стр.27 — gRPC-клиент только при `recaptcha_key` (иначе None). Корректно.
- Запуск архитектором: backend `:8000` -> `GET /api/status` = 200 `{"mode":"simple",...}`,
  лог `GeminiLive init: model=gemini-3.1-flash-live-preview, mode=api_key`. Frontend `:5173` (Vite), прокси на бэкенд жив.
  Прибран осиротевший node на 5173 из сессии кодера; фронт перезапущен на штатном 5173.
- Статус: код принят; **финальная приёмка ждёт голосовой тест пользователя в браузере** (иврит).
  Задание 002 НЕ архивируем до подтверждения живого диалога.

## 2026-06-21 — Живой тест: диагностика 2 багов; архив 002, открыто 003
- Кто: архитектор (чат). Пользователь дважды протестировал в браузере — голоса нет.
- Дефолты в UI подтверждены пользователем: RU Русские -> IL Иврит (скрин). Фронт ок.
- Диагностика по логу бэкенда (после запуска с PYTHONUTF8=1 ошибки перестали маскироваться):
  1. **cp1252-краш**: `print()` с эмодзи (`⚡️`/`🔧`) -> UnicodeEncodeError -> сессия падала (DEV-NOTES п.4).
     Обойдено запуском с `PYTHONUTF8=1`.
  2. **Реальная причина тишины**: `1007 Invalid JSON ... Unknown name "proactivity" at 'setup'`.
     Бэкенд шлёт поле `proactivity`, которого нет в схеме developer-API для `gemini-3.1-flash-live-preview`.
     ВАЖНО: соединение с Gemini устанавливается, промпт содержит иврит/русский -> ключ+модель РАБОЧИЕ.
- Архив: задание 002 принято (код-миграция) -> `tasks/done/002-switch-to-gemini-api-key/`.
- Новое задание 003 (`tasks/TASK.md`): (1) `sys.stdout/stderr.reconfigure(utf-8)` в `main.py` —
  постоянный фикс кодировки; (2) не слать `proactivity` в `config_args` (`gemini_live.py`);
  (3) перезапуск + проверка, что 1007 пропал. Голосовой тест — за пользователем.
- Операционно: архитектор остановил свои фоновые серверы (8000/5173), чтобы кодер работал на чистых портах.

## 2026-06-21 — Голос работает; приёмка 003; подготовка к публикации в GitHub
- Кто: архитектор (чат). Пользователь подтвердил: живой голосовой диалог на иврите идёт (были задержки/обрывы — квота/preview-модель, не дефект).
- Проверка 003: `main.py` reconfigure(utf-8) стр.17-18; `proactivity` в `server/` = 0; backend 200, без 1007.
  **Задание 003 ПРИНЯТО** -> `tasks/done/003-fix-live-voice/`.
- Цель пользователя: запушить всё в свой репо https://github.com/Yuri-Sverdlov/IMMERSIVE-LANGUAGE-API-IVRIT
  (пустой) для развёртывания на другом ПК.
- Подготовка архитектора к публикации:
  - Убран чужой `app/.git` (origin был ZackAkil) -> проект «сплющен» в один репозиторий.
  - Создан корневой `.gitignore` (защита `.env`/ключа в 2 слоя; игнор node_modules/venv/dist/.claude).
  - Создан `DEPLOY.md` (инструкция развёртывания на другой машине, Windows).
  - Обновлён `app/.env.example` под режим ключа (GEMINI_API_KEY, MODEL=gemini-3.1-flash-live-preview).
  - app/.gitignore уже игнорирует `.env` -> ключ защищён.
- Новое задание 004 (git, ИСКЛЮЧЕНИЕ): кодер делает `git init` в корне, remote=репо пользователя,
  проверяет что `app/.env` НЕ застейджен, commit + push в main, сверяет `git ls-remote`.
  Авторизация HTTPS -> PAT в Windows Credential Manager (если нет — блокер для пользователя).

## 2026-06-21 — Публикация в GitHub выполнена; приёмка 004
- Кто: архитектор (чат), проверка отчёта кодера.
- Кодер выполнил git init + push. Проверено независимо:
  - remote = Yuri-Sverdlov/IMMERSIVE-LANGUAGE-API-IVRIT; `HEAD` = `53afbd94...` = `ls-remote` main (пуш долетел).
  - `app/.env` НЕ в индексе (только `.env.example`); node_modules/venv/dist не отслеживаются.
  - Страж `git grep` префикса ключа по дереву коммита -> 0 совпадений. **Ключ не утёк.**
  - 67 файлов в коммите.
- **Задание 004 ПРИНЯТО** -> `tasks/done/004-git-publish-github/`.
- Состояние: проект опубликован, разворачивается по `DEPLOY.md`. Активных заданий нет.
- Возможные следующие темы (по желанию пользователя): кастомные квесты (напр. «шашки с внуком»),
  регулятор уровня иврита, доступ с телефона по локальной сети, тюнинг задержек/квоты Live API.

## 2026-06-21 — Разбор обрывов; лимит сессии 7 минут
- Кто: архитектор (чат). Разбор причин обрывов/задержек по коду.
- Вывод: обрывы = жёсткий таймаут сессии `SESSION_TIME_LIMIT` (default 180с),
  `asyncio.wait_for(run_session(), timeout=...)` в `main.py:316`. НЕ лимит токенов (его в коде нет;
  логирование токенов закомментировано). Лимита по сценарию тоже нет.
- Действие: в `app/.env` добавлен `SESSION_TIME_LIMIT=420` (7 мин). Перезапуск -> `/api/auth`
  возвращает `session_time_limit: 420`. Подтверждено.
- НОВАЯ ГРАБЛЯ (важно): `.env` ДОЛЖЕН быть ASCII. `starlette.Config(env_file=".env")` читает файл в
  cp1252 на этом ПК -> кириллический комментарий дал `UnicodeDecodeError` при старте. Исправлено:
  комментарии в `.env` и `.env.example` переведены в ASCII. (PYTHONUTF8 бы помог, но reconfigure stdout
  не влияет на чтение env-файла при импорте.)
- ВНИМАНИЕ для репозитория: закоммиченный на GitHub `app/.env.example` сейчас содержит старые
  кириллические комментарии (сломают redeploy при копировании в .env). НУЖЕН коммит ASCII-версии —
  включить в следующее git-задание.
- Открытый вопрос пользователю: логирование токенов — в терминал (просто) или счётчик в браузере (доработка UI)?
- VAD silence 2с пользователь решил НЕ менять.

## 2026-06-21 — DEV-NOTES п.7; задание 005 (токен-лог + коммит); передача терминала пользователю
- Кто: архитектор (чат).
- DEV-NOTES: в глобальный `G:\_My_Programming\DEV-NOTES.md` добавлен п.7 (Gemini Live API + грабли
  .env/cp1252), дата обновлена. Копия в проекте синхронизирована (в репозиторий уйдёт коммитом 005).
- Пользователь хочет видеть расход токенов в ВИДИМОМ терминале. Решение: логирование включаем в коде
  (закомментировано в `gemini_live.py`), вывод строкой `[TOKENS] total=... prompt=... response=...` (ASCII).
  Где смотреть: в консоли бэкенда после каждого ответа модели.
- Задание 005 (git-разрешено): этап1 — раскомментировать токен-лог (ASCII); этап2 — проверить старт;
  этап3 — закоммитить+запушить накопленное (gemini_live.py, .env.example ASCII-фикс, DEV-NOTES п.7).
  Страж: `app/.env` НЕ коммитить.
- Операционно: архитектор остановил свои фоновые серверы (8000/5173 свободны). Дальше бэкенд/фронт
  пользователь (или кодер при тесте) запускает в своём видимом терминале.

## 2026-06-21 — Приёмка 005 (токен-лог + коммит)
- Кто: архитектор (чат), проверка отчёта.
- Проверено: `gemini_live.py:159-162` -> `print("[TOKENS] total=... prompt=... response=...")` (ASCII);
  поля usage_metadata верны; backend 200. Коммит `338a6de` запушен, `ls-remote main` = HEAD.
  Страж `.env` чист; `git grep` уникального фрагмента ключа -> 0 (совпал лишь префикс `AQ.A` в DEV-NOTES).
- **Задание 005 ПРИНЯТО** -> `tasks/done/005-token-logging/`. Активных заданий нет.
- Дальше: пользователь запускает backend в своём терминале и смотрит `[TOKENS]` при миссии.
- Примечание: `tasks/REPORT.md` остаётся изменённым в рабочем дереве (отчёт 005) — норма.

## 2026-06-21 — Фикс French->Hebrew, BAT launcher; задание 006 (git)
- Архитектор: миграция stale French в localStorage, fallback Hebrew/Russian в view-chat.js;
  start-immergo.bat + run-backend/frontend.cmd + очистка портов 8000/5173.
- Пользователь подтвердил: всё работает.
- Задание 006 (git): кодер коммитит и пушит перечисленные файлы; `app/.env` и файл `new` не коммитить.

## 2026-06-21 — Приёмка 006 (Hebrew fix + launcher + push)
- Архитектор проверил независимо: миграция French, fallback Hebrew/Russian, BAT+port cleanup,
  коммит `1df6231` = remote main, ключ не утёк, `new` не в репо.
- **Задание 006 ПРИНЯТО** -> `tasks/done/006-hebrew-fix-launcher-git/`. Активных заданий нет.

## 2026-06-21 — План следующей сессии (007): config, квесты, quick start
- Кто: архитектор (чат), по запросу пользователя.
- Зафиксировано в `CONTEXT.md` раздел «План следующей сессии»:
  1. **Config-файл** (`app/immergo.config.json`): родной/целевой язык (пример: RU + EN или RU + HE),
     `session_time_limit_seconds` (сейчас 420), `silence_duration_ms` (сейчас 2000). Секреты — в `.env`.
  2. **Длительность сессии** и **время тишины** — читать из config, не хардкодить в JS/.env.
  3. **Шаблон квеста:** `app/src/data/mission.template.json` + `docs/HOW-TO-ADD-QUEST.md`.
  4. **QUICKSTART.md** — для новичка-пользователя (что нажимать, возможности), отдельно от DEPLOY.md.
- Рекомендованный порядок: config -> session/VAD -> quest docs -> quick start -> git.
- `tasks/TASK.md`: активного задания нет; на старте следующей сессии архитектор пишет TASK 007.

## 2026-06-21 — QUICK_START.md (п.5 плана 007)
- Кто: архитектор (чат). Создан **`QUICK_START.md`** — руководство для пользователя-новичка
  (запуск через start-immergo.bat, экраны, режимы, микрофон, токены, ограничения, FAQ).
- В `DEPLOY.md` добавлена ссылка на QUICK_START. В CONTEXT п.5 отмечен как выполненный.
- Не закоммичено (git — по отдельному заданию пользователя).

## 2026-06-22 — Слияние с локальной копией (этот ПК); приёмка sync-отчёта
- Кто: архитектор (чат), проверка отчёта кодера на этом ПК.
- Контекст: локальная папка `IMMERSIVE-ROLE-PLAYING-DIALOGUES` связана с GitHub-форком;
  структура `app/` + `start-immergo.bat` на месте; push `66bd2ee` = origin/main.
- Проверено: `[TOKENS]` в gemini_live.py; Hebrew/Russian UI; uvicorn + /api/status OK;
  `app/.env` не в git.
- Хвост слияния: облачные md (`КАК-ПОЛЬЗОВАТЬСЯ...`, `ОБЛАКО-Immergo...`) только в `.backup_local/` —
  вернуть в корень (TASK-006).
- Дальше: TASK-006 для кодера; затем фаза 2 (dual Vertex + API key) или задание 007 (config).

## 2026-06-22 — Приёмка TASK-006 (облачные md + CONTEXT)
- Кто: архитектор
- Проверено: md в корне; CONTEXT (путь, Cloud Run, иврит); commit `12bc273` = origin/main
- **Фаза 1 слияния ЗАКРЫТА**
- **2026-06-22:** venv создан на ПК2; backend+frontend проверены (8000/5173 OK)
- **2026-06-22:** пользователь подтвердил локальный запуск (ключ Gemini, голос OK)
- **2026-06-22:** открыто TASK-007 — immergo.config.json

## 2026-06-22 — Приёмка TASK-007 (immergo.config.json)
- Кто: архитектор (чат), отчёт кодера (Claude Code CLI, ~7 мин).
- Commit: `f60c7df` = origin/main (8 файлов).
- Проверено независимо:
  - `app/immergo.config.json` — RU/HE, 420s, 2000ms.
  - `load_immergo_config()` в `config_utils.py` — merge с дефолтами, WARNING при битом JSON.
  - `GET /api/config` → 200, поля совпадают; `POST /api/auth` → `session_time_limit: 420`.
  - Frontend: `view-missions.js` fetch config; `view-chat.js` + `setSilenceDurationMs()` перед connect.
  - `app/.env` не в git.
- Вердикт: **ПРИНЯТО**. Голосовой тест VAD/языков — на пользователя (инкогнито / смена config).
- Архив: `tasks/done/007-immergo-config/`.

## 2026-06-22 — Обсуждение: скорость речи ИИ (план TASK-008)
- Кто: архитектор + пользователь.
- Запрос: скорость ответа **0,7 … 1,5** (1,0 = как сейчас).
- Вывод: Gemini Live API **не экспонирует** `speaking_rate` в SpeechConfig; `silence_duration_ms` — это VAD, не темп речи.
- Решение: поле `speech_playback_rate` в `immergo.config.json`, реализация **на клиенте** в `AudioPlayer` / playback worklet.
- Зафиксировано в `CONTEXT.md` раздел «План TASK-008».

## 2026-06-23 — Приёмка TASK-008 (speech_playback_rate)
- Кто: архитектор (чат), отчёт кодера (терминал).
- Сделано: `speech_playback_rate` в `immergo.config.json`; clamp в `config_utils.py`;
  поле в `GET /api/config`; resample в `playback.worklet.js`; `AudioPlayer.setPlaybackRate()`;
  wiring в `view-chat.js`; строка в `QUICK_START.md`.
- Проверено независимо:
  - grep по ключевым файлам — все точки на месте.
  - `npm run build` — OK (134ms).
  - `load_immergo_config()` — `rate= 1.0`.
- Вердикт: **ПРИНЯТО**. Голосовой тест 0.7/1.5 — на пользователя. Коммита нет (не было git-задания).
- Архив: `tasks/done/008-speech-playback-rate/`.
- Дальше: TASK-009 или git-задание на commit 008.

## 2026-06-24 — Приёмка TASK-009 (sync ПК1 с GitHub)
- Кто: архитектор (чат), отчёт кодера (терминал).
- Задание: pull с `origin/main` (компьютер 2 -> этот ПК), сохранить локальный `app/.env`.
- Проверено независимо:
  - `git rev-parse HEAD` = `origin/main` = `b642136e3a0216a0e7c3ea6cd8ef99ac844f4d92`.
  - `app/immergo.config.json` — RU/HE, 420s, 2000ms, rate 1.0.
  - `git ls-files` — только `app/.env.example`.
  - Отчёт: fast-forward 42c9ee4 -> b642136, конфликтов нет, `/api/status` 200.
- Вердикт: **ПРИНЯТО**. Голосовой тест на ПК1 — на пользователя.
- Архив: `tasks/done/009-sync-from-github/`.
- Дальше: шаблон квеста (`mission.template.json` + `HOW-TO-ADD-QUEST.md`).

## 2026-06-24 — Приёмка TASK-010 (шаблон квеста)
- Кто: архитектор (чат), отчёт кодера (терминал).
- Сделано: `app/src/data/mission.template.json`, `docs/HOW-TO-ADD-QUEST.md`, ссылка в `QUICK_START.md`.
- Проверено независимо:
  - шаблон — валидный JSON, 5 полей;
  - HOW-TO — все 7 разделов, пример «шашки», difficulty косметический;
  - `npm run build` — OK (227ms);
  - `missions.json` не тронут.
- Вердикт: **ПРИНЯТО**. Тест добавления квеста — на пользователя.
- Архив: `tasks/done/010-quest-template/`.
- Дальше: git push (010 на GitHub) или Cloud Run redeploy.

## 2026-06-24 — Приёмка TASK-011 (git push 010)
- Кто: архитектор (чат), отчёт кодера (терминал).
- Commit: `e4339e7` = `origin/main` (13 файлов: mission.template, HOW-TO, QUICK_START, архивы 009/010, CONTEXT, PROJECT_LOG).
- Проверено: `git ls-remote` = локальный HEAD; `.env` не в commit; push OK.
- Вердикт: **ПРИНЯТО**.
- Архив: `tasks/done/011-git-push-010/`.
- Дальше: Cloud Run redeploy или `git pull` на втором ПК.

## 2026-06-24 — Идея пользователя: GUI «скорость диалога» (план TASK-012)
- Кто: пользователь + архитектор (чат).
- Запрос: вынести управление темпом речи LLM из config на экран разговора; менять **во время** диалога.
- UI: подпись «скорость диалога», поле значения, кнопки ▲/▼, шаг 0,5.
- Диапазон в формулировке пользователя: 6,0…1,5 и при «+» 6,0→6,5→7,0 — **не совпадает** с `speech_playback_rate` 0,7–1,5; уточнить перед TASK.
- Место на экране (архитектор): под title/desc миссии, над визуализатором (`view-chat.js`).
- Зафиксировано в `CONTEXT.md` → «План TASK-012». Код не начинать до `tasks/TASK.md`.
- Дальше: ещё одна идея от пользователя; затем TASK-012 после уточнения шкалы.

## 2026-06-24 — Уточнение TASK-012 (шкала и сохранение)
- Кто: пользователь + архитектор (чат).
- Шкала: **0,6 … 1,5**, шаг **0,5** (▲/▼); 1,0 = норма. Опечатка «6.0» снята.
- Сохранение: выбранное значение **persist** — в текущей voice-сессии и при **новом** заходе (localStorage, fallback config).
- При реализации: расширить clamp TASK-008 с 0,7 до 0,6 на backend + клиенте.
- Зафиксировано в `CONTEXT.md` → «План TASK-012».

## 2026-06-24 — Идея пользователя: «Микрофон: вкл/выкл» (план TASK-013)
- Кто: пользователь + архитектор (чат).
- Запрос: кнопка «Микрофон: включен/отключен» + кружок (красный=on, серый=off).
- При off: LLM не слышит комнату; **VAD / silence 2 s отключён**.
- При on: восстановить отправку аудио и `silence_duration_ms` из config.
- UI (черновик): над user visualizer или у mic-CTA (`view-chat.js`).
- Техника: pause PCM в `AudioStreamer`; VAD via `automaticActivityDetection.disabled` — проверить mid-session.
- Зафиксировано в `CONTEXT.md` → «План TASK-013». Код не начинать до `tasks/TASK.md`.

## 2026-06-24 — Уточнение UI: одна панель (012 + 013)
- Кто: пользователь + архитектор (чат).
- Скорость диалога и «Микрофон: вкл/выкл» — **на одной панели, рядом** (не в разных зонах экрана).
- Место: под desc миссии, над горизонтальными линиями / зоной диалога (`view-chat.js`, `.session-control-panel`).
- Зафиксировано в `CONTEXT.md` → «Единая панель управления сессией».

## 2026-06-24 — TASK-012+013 записано в tasks/TASK.md
- Кто: архитектор (чат).
- Одно задание кодеру: единая `.session-control-panel` (скорость + mic), localStorage rate, clamp 0,6–1,5, mute без persist.
- Git: **не** коммитить до команды пользователя. Кодер — по фразе «дай задание в терминал».
- Дальше: реализация кодером → приёмка → git-задание при необходимости.

## 2026-06-24 — Приёмка TASK-012+013 (панель сессии)
- Кто: архитектор (чат), кодер (терминал), голосовой тест пользователя.
- Сделано: `.session-control-panel`, скорость (localStorage, clamp 0,6–1,5), mic mute + VAD attempt, QUICK_START.
- Доработки: шаг скорости 0,025; `languagePrefs.js` — RU→HE при старте и между миссиями.
- Пользователь: mic OK; языки OK после фикса.
- Архив: `tasks/done/012-session-control-panel/`.
- Дальше: git commit + push (конец сессии).

## 2026-06-24 — Git push сессии (012 + фиксы)
- Кто: архитектор/кодер по команде пользователя.
- Commit: session control panel, languagePrefs, docs, архивы 011/012.
- Исключено: `app/.env.backup`, `new`, `tasks/done/007-quick-start-git/`.
